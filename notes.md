# To do:

* CMD line arguments

    + -d/-e -- ch functions 
    + -f in out -- files to read and write from 
    + -o -- offline (no files)
    + -h -- help
    + -c -- call (if present = 'Y')
* Add more than one line